<h1><%= @event.name %></h1>

<%= link_to 'Edit', edit_event_path(@event) %> | <%= link_to 'Delete', event_path(@event), method: :delete, data: {confirm: "Are you sure that you want to delete this event #{@event.name} on the #{@event.date}? This cannot be undone!"} %>


<%= render 'single_event', event: @event %>
